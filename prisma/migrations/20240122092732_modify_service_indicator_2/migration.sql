BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[ServiceIndicator] ADD CONSTRAINT [ServiceIndicator_quantity_df] DEFAULT 0 FOR [quantity];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
